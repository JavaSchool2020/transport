Реализовать консольное приложение, которое может создать один из типов автотранспорта : автомобиль, мотоцикл или автобус.
В приложении должны быть реализованы
1. Transport, который не может иметь инстансов (нельзя создать его объекты). Все его наследники должны иметь возможность возвращать VIN, дату выпуска, тип транспорта (автомобиль, мотоцикл или автобус), размеры (длину, ширину, высоту), максимальную скорость. Все свойства должны быть неизменяемыми.
2. Car, Motorcycle и Bus, которые являются наследниками Transport. При создании обЪекта данных классов обязательно должны присваиваться значения VIN и дата выпуска. Они не могу изменяться.
3. Параметризованный (обобщенный) класс-билдер (класс сборщик объекта, паттерн Builder) TransportBuilder - абстрактный класс. Ограничить тип параметризации наследниками Transport.
Данный билдер призван устанранить недостаток анти-паттерна "телескопического конструктора".
Он должен уметь собирать транспорт устанавливая необходимые свойства.Для построения каждого типа транспорта нужно создать отдельный подтип TransportBuilder, например, для создания автомобиля

new CarBuilder().vin("vin").<установить другие свойства>.build();
